#include "main.h"

/**
 * add - adds two integers and returns the result
 * @first: forst inputed number
 * @second: second number input
 * Return: The total of the numbers
 */

int add(int first, int second)
{
	int total = first + second;

	return (total);
}
